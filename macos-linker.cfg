# Conditional FPC options for Lazarus examples.
#
# Recent macOS/Xcode linkers can fail Cocoa LCL builds with:
#   ld: malformed method list atom ... cocoawsextctrls.o
#
# Keep the workaround target-scoped so Linux and Windows builds ignore it.
#ifdef darwin
-k-ld_classic
#endif
